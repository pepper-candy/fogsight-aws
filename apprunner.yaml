version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install fastapi uvicorn openai requests
run:
  command: uvicorn bridge:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
